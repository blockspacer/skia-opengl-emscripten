#pragma once

#include "build/buildflag.h"

#define BUILDFLAG_INTERNAL_POSIX_AVOID_MMAP() (1)
#define BUILDFLAG_INTERNAL_ENABLE_SWIFTSHADER() (0)
#define BUILDFLAG_INTERNAL_USE_DAWN() (0)
#define BUILDFLAG_INTERNAL_USE_EGL_ON_MAC() (0)
#define BUILDFLAG_INTERNAL_USE_STATIC_ANGLE() (0)
